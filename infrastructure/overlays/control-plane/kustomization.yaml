apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# "control-plane" is the infrastructure profile for hub clusters.
# For now it reuses the same baseline as resource-plane (namespaces + RBAC).
# Add control-plane-specific infra here once defined (e.g. cert-manager,
# an ingress controller, or an External Secrets Operator install shared by
# all hub clusters).
resources:
  - ../../base
