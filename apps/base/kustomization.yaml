apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Universal apps catalog (deployable to all cluster groups)
# Each group's overlay (control-plane, resource-plane, etc.) decides
# which of these apps to include via its own kustomization.yaml

resources:
  - ./podinfo
  - ./whoami
  # - ./external-secrets-operator  # commented out: not yet deployed
